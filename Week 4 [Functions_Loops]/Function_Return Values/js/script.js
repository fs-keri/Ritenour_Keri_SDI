/**
 * Keri Ritenour
 * 5/26/15
 * Section 1
 * Functions - Returning Values
 */

var total = calcArea(30, 20); //whatever this returns store it in the variable

function calcArea(w, h){
    var area = w * h;
    return area; //is being returned to calcArea
}

console.log(total);